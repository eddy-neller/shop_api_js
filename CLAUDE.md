# CLAUDE.md

> Source unique des conventions du projet : **`AGENTS.md`** (lu par Claude, Copilot, Cursor…).
> Ce fichier ne fait que l'importer pour éviter toute duplication / dérive.
>
> Les règles spécifiques à une couche vivent dans le `AGENTS.md` du dossier correspondant
> (`src/domain/`, `src/application/`, `src/infrastructure/`, `src/presentation/`) et sont chargées
> à la demande quand on travaille dans cette couche.

@AGENTS.md
